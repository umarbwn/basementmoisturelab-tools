import type {Metadata} from "next";
import {Geist, Geist_Mono} from "next/font/google";
import "./globals.css";
import React from "react";

const geistSans = Geist({
    variable: "--font-geist-sans",
    subsets: ["latin"],
});

const geistMono = Geist_Mono({
    variable: "--font-geist-mono",
    subsets: ["latin"],
});

export const metadata: Metadata = {
    title: "Basement Moisture Lab Tools",
    description: "Use our basement moisture lab tools to analyze humidity, condensation risk, and moisture levels. Free online calculators to diagnose basement dampness.",
    verification: {
        google: 'xw6_rzf7p41bZttzutsaGpDwnaq0YfAnsW0zvlA9MUA'
    }
};

export default function RootLayout({
                                       children,
                                   }: Readonly<{
    children: React.ReactNode;
}>) {
    return (
        <html lang="en">
        <body
            className={`${geistSans.variable} ${geistMono.variable} antialiased`}
        >
        {children}
        </body>
        </html>
    );
}
